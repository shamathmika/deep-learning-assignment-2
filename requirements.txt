# ============================================================
# Inference Optimization
# Platform: Apple Silicon (M5 Pro), MPS backend
# Python: 3.14 (Homebrew)
# ============================================================

# --- Core ML: PyTorch (MPS-enabled build for Apple Silicon) ---
torch==2.11.0
torchvision==0.26.0

# --- Object Detection Models ---
# Provides YOLOv8 and RT-DETR via unified Ultralytics API
ultralytics==8.4.37

# --- Inference Acceleration ---
# TorchScript — required method #1 (built into torch, no extra install)
# OpenVINO — required method #2
openvino==2026.1.0
# ONNX model serialization format
onnx==1.21.0
# ONNX Runtime with CoreML Execution Provider (Apple Neural Engine)
onnxruntime==1.24.4

# --- FastAPI Backend ---
fastapi==0.135.3
uvicorn==0.44.0           # ASGI server
python-multipart==0.0.26  # required for file upload endpoints
python-dotenv==1.2.2      # environment variable management

# --- Image / Video Processing ---
opencv-python-headless==4.13.0.92
pillow==12.2.0

# --- Evaluation ---
pycocotools==2.0.11       # COCO-format mAP calculation

# --- Utilities ---
numpy==2.4.4
requests==2.33.1
